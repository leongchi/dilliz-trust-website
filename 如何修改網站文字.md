# 📝 如何修改網站文字 (How to Edit Website Text)

## 最簡單的方法：修改 translations.json

所有網站顯示的文字都集中在一個 JSON 檔案中：

```
client/src/lib/translations.json
```

### 步驟：

1. **用任何文字編輯器打開** `client/src/lib/translations.json`
2. **搜尋您想修改的文字**（用 Ctrl+F 或 Cmd+F）
3. **修改對應語言的文字**：
   - `"zh"` = 繁體中文
   - `"cn"` = 簡體中文
   - `"en"` = English
4. **儲存檔案**
5. **重新打包**（見下方「如何重新打包」）

### 範例：

如果您想把「尊享定存特惠收益率」改為「尊享特惠收益」，找到：

```json
"尊享定存特惠收益率"
```

改為：

```json
"尊享特惠收益"
```

---

## 其他頁面的內嵌文字

部分文字直接寫在頁面檔案中（而非 translations.json），這些檔案位於：

| 頁面 | 檔案路徑 |
|---|---|
| 首頁 | `client/src/pages/Home.tsx` |
| 服務範圍 | `client/src/pages/Services.tsx` |
| 關於我們 | `client/src/pages/About.tsx` |
| 會員計劃 | `client/src/pages/Membership.tsx` |
| 真實案例 | `client/src/pages/Cases.tsx` |
| 常見問題 | `client/src/pages/FAQ.tsx` |
| 聯絡我們 | `client/src/pages/Contact.tsx` |
| 頂部導航+頁尾 | `client/src/components/Layout.tsx` |
| 隱私政策 | `client/src/pages/Privacy.tsx` |
| 使用條款 | `client/src/pages/Terms.tsx` |

在這些檔案中，搜尋您想修改的中文文字，直接替換即可。

---

## 如何重新打包（生成 cPanel 部署檔案）

### 前提條件：
- 安裝 [Node.js](https://nodejs.org/) (v18 或以上)
- 安裝 pnpm: `npm install -g pnpm`

### 打包步驟：

```bash
# 1. 進入項目目錄
cd dilliz-trust-website

# 2. 安裝依賴（首次或更新後執行）
pnpm install

# 3. 打包生產版本
npx vite build

# 4. 打包完成後，部署檔案在：
#    dist/public/
```

### 上傳到 cPanel：

1. 將 `dist/public/` 資料夾內的**所有檔案**上傳到 cPanel 的 `public_html/` 目錄
2. 確保 `.htaccess` 檔案也一併上傳（這是 SPA 路由必需的）
3. 完成！

---

## 注意事項

- 修改 JSON 時請確保格式正確（不要漏掉引號或逗號）
- 建議使用 [VS Code](https://code.visualstudio.com/) 編輯，它會自動提示 JSON 格式錯誤
- 修改後請在本地測試（`npx vite`）確認無誤再上傳
